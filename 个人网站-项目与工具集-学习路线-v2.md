# 个人网站、项目中心与工具集：学习路线 v2

> 更新日期：2026-09-07
>
> 当前阶段：核心全栈功能已完成，进入真实内容、部署核验与质量保障阶段。

这份文档既是学习路线，也是项目进度表。它只把代码和数据库迁移中可验证的内容标记为完成；域名、线上部署和外部任务等状态，需要在目标环境中另外确认。

## 1. 产品定位

MY SPACE 不是求职简历模板，而是一个长期个人入口：

```text
公开内容空间
├─ AI 新闻简报
├─ 项目列表与详情
└─ 工具收藏与分类

个人管理空间
├─ 项目管理
├─ 工具管理
└─ 工具分类管理
```

当前用户模型是 Visitor + Admin（游客 + 管理员）：游客无需注册，只浏览允许公开的内容；管理员登录后维护数据。普通用户账号、评论、多人协作等功能只有出现明确需求后才考虑。

## 2. 当前实现快照

| 能力 | 状态 | 实现位置或说明 |
|---|---|---|
| Next.js App Router（应用路由）基础 | 已完成 | `app/` |
| 响应式公共布局与导航 | 已完成 | `components/Header*`、`app/globals.css` |
| AI 新闻首页与列表 | 示例版 | `components/news/NewsHome.tsx`；尚无真实来源和采集 |
| 项目列表、分页与详情 | 已完成 | `app/projects/`、`lib/project-repository.ts` |
| 工具搜索、筛选与外链 | 已完成 | `app/tools/`、`components/ToolExplorer.tsx` |
| Supabase 数据库 | 已完成 | `supabase/migrations/` |
| 管理员登录和服务端授权 | 已完成 | `app/login/`、`lib/auth/admin.ts` |
| 工具 CRUD（增删改查） | 已完成 | `app/admin/tools/` |
| 项目 CRUD | 已完成 | `app/admin/projects/` |
| 工具分类 CRUD | 已完成 | `app/admin/categories/` |
| 游客可见 / 仅登录可见 | 已完成 | 项目、工具及对应数据库策略 |
| 未保存提醒、确认弹窗、错误状态 | 已完成 | `components/admin/` |
| 线上部署和域名 | 待核验 | 本地仓库无法证明线上状态 |
| 自动化测试 | 未开始 | 当前没有单元测试或端到端测试配置 |

## 3. 已完成的学习阶段

### Phase 1：前端基础

- 理解 Server Component（服务端组件）与 Client Component（客户端组件）的职责。
- 使用 TypeScript 定义项目、工具、表单和数据库返回值。
- 使用 CSS Modules 与全局 CSS 变量建立响应式界面。
- 完成首页、项目、工具等公共路由以及加载、错误和 404 状态。

### Phase 2：数据与内容建模

- 区分项目介绍页与独立部署的项目本体。
- 为项目建立 `slug`、状态、介绍、外链、标签和亮点等数据。
- 为工具建立名称、网址、描述、分类、标签和常用状态。
- 使用 Supabase PostgreSQL（关系型数据库）替代纯静态数据，同时保留本地回退数据用于学习和容错。

### Phase 3：认证、权限与后台

- 使用 Supabase Auth（身份认证）完成单管理员登录。
- 理解 Authentication（身份认证）与 Authorization（权限控制）的区别。
- 在服务端读取和写入数据前重新验证管理员，不依赖前端隐藏。
- 完成项目、工具和工具分类管理。
- 使用 Server Action（服务端操作）提交表单并在失败时保留输入。
- 为永久删除、批量可见性和未保存内容提供应用内确认。

### Phase 4：数据可见性与一致性

- 项目和工具统一使用 `hide_from_guests` 表达游客可见性。
- 将游客可见性与发布状态、项目进度、首页推荐分离。
- 公开列表、详情和顶部菜单都按当前账号可见范围读取。
- 新建内容默认游客可见，旧隐藏数据通过迁移保持原有含义。

## 4. 下一阶段路线

### Step 1：建立自动化测试

目标：把目前依赖人工检查的关键规则变成可重复验证的测试。

建议顺序：

1. 为 `lib/project-form.ts`、`lib/tool-form.ts` 和状态转换函数增加单元测试。
2. 为登录失败、表单校验、删除确认和可见性批量操作增加组件或集成测试。
3. 使用 Playwright（浏览器端到端测试工具）覆盖游客浏览、管理员登录和一条完整 CRUD 流程。
4. 在持续集成中运行 lint（代码规范检查）、类型检查、测试和生产构建。

为什么先做测试：项目已经包含权限、永久删除和数据库写入。继续添加功能而没有回归保护，会让“改好一处、破坏另一处”的风险快速上升。

### Step 2：把 AI 新闻从示例升级为真实内容

需要先完成数据与来源设计，再做自动采集：

- 新闻表至少记录标题、摘要、原文 URL、来源、发布时间、分类、抓取时间和发布状态。
- 每一条内容必须能追溯到真实来源，不伪造相对时间、作者或历史归档。
- 采集失败、重复新闻、来源限流和人工下线都要有明确处理方式。
- 自动摘要涉及外部模型时，要记录成本、失败恢复和内容安全边界。
- 首页只展示已审核或满足明确发布规则的内容。

第一步建议先做“管理员手动录入新闻”，验证数据模型和页面；自动采集放在其后。这样能先验证产品流程，避免一开始就被定时任务和外部 API（接口）复杂度拖住。

### Step 3：部署与域名验收

1. 确认托管平台和生产 Supabase 项目。
2. 在平台中配置三个环境变量，不把真实值写进仓库。
3. 对生产数据库应用迁移，并核对 Row Level Security（行级安全策略）。
4. 运行生产构建后部署，检查登录回调地址和 Cookie（浏览器会话数据）。
5. 配置域名、HTTPS（加密传输）与 DNS（域名解析）。
6. 分别以游客和管理员身份验收公开、仅登录可见和永久删除行为。

部署完成的判断标准不是“平台显示成功”，而是实际域名上的主要流程、权限与错误状态都能工作。

### Step 4：内容与运维完善

- 用真实项目与工具数据替换演示数据。
- 为数据库建立备份和恢复演练，特别是永久删除上线前。
- 增加错误监控、访问日志边界和可用性监控。
- 根据真实使用反馈决定是否增加图片上传、草稿、归档或搜索参数持久化。

## 5. 暂不开发的功能

以下功能目前没有明确收益，先不加入：

- 普通用户注册、个人收藏和评论。
- 多角色、多人协作和复杂权限矩阵。
- 把独立 AI 项目的完整代码嵌入本网站。
- 为少量内容引入额外 ORM（对象关系映射工具）或第二套后端。
- 没有可靠来源和审核流程的自动新闻发布。
- 无法真实恢复的“撤销删除”按钮。

控制范围的原因是：当前项目的价值来自“个人入口真正可维护”，而不是功能数量。

## 6. 推荐学习方式

每次只处理一个可验证的小目标：

```text
理解概念
→ 找到项目中的现有实现
→ 修改一个完整流程
→ 运行静态检查与构建
→ 在浏览器验证成功和失败状态
→ 更新文档与变更记录
```

遇到命令时，不只记住命令本身，还要理解它读取什么、修改什么、成功输出是什么、失败后如何恢复。涉及数据库迁移、永久删除和生产部署时，必须先确认目标环境与影响范围。

## 7. 当前完成标准

下一次可以称为“可发布版本”之前，至少应满足：

- `npm run lint` 通过。
- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 关键表单和权限路径具备自动化测试。
- 390px、768px、1024px 和 1440px 宽度下没有意外页面横向滚动。
- 键盘可以完成主要操作，焦点清晰，弹窗关闭后焦点回到触发按钮。
- 游客无法读取仅登录可见内容，非管理员无法进入后台或调用写操作。
- 新闻示例继续清楚标记；接入真实新闻后，每条内容都有来源。
- 部署、域名、环境变量和数据库迁移已在生产环境单独核验。

## 8. 文档分工

- `README.md`：告诉开发者项目是什么、怎样运行、当前有什么限制。
- 本文：记录学习进度、下一阶段和范围控制。
- `DESIGN.md`：记录长期视觉方向与设计令牌。
- `UX-CONTRACT.md`：记录跨页面必须一致的交互行为。
- `CHANGELOG.md`：只记录已经完成的重要变化。
- `docs/learning-notes/`：保留逐日学习材料和概念解释。

当代码与文档冲突时，先判断代码是否实现错误，不能为了让文档“看起来一致”就把偶然偏差写成新规则。
