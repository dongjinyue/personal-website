# 变更说明

本文件记录已经完成并可从代码或数据库迁移中验证的重要变化。计划中的功能放在学习路线，不在这里提前宣布。

## 待发布：知识库读取与检索工作台

- 新增 `/knowledge` 知识库索引：支持关键词、分类、标签、排序和服务端分页，筛选状态可通过网址恢复与分享。
- 搜索输入支持中文 IME（输入法编辑器）组合输入、300ms 防抖、即时清除与规范 URL 跳转；空知识库、无结果、加载和读取失败分别提供明确状态。
- 顶部导航增加“知识库”入口；游客只会收到公开已发布笔记，管理员可同时浏览私密与草稿内容并看到状态标识。
- 新增知识库笔记阅读页：提供本文目录、出链/反向链接、相邻笔记导航、代码复制和可访问的图片大图查看；不存在与无权访问统一返回不泄漏资源状态的 404 页面。
- 公开笔记中的私密目标与不存在目标链接统一显示为普通文字；代码展示保留高亮并按原始代码文本复制，嵌套图片链接拆为独立操作，大图加载失败显示提示。
- 附件读取只把“固定提交中确实缺少目标路径”映射为未找到；非法提交标识、无效提交、Git 对象损坏及其他执行故障统一以结构化错误向上抛出。
- 搜索摘要和高亮在整体 NFKC（兼容形式）及整体大小写归一化匹配后映射回原文，组合字符、希腊语上下文小写与正文后段命中不会丢失原始文本。
- 详情关系摘要改用明确字段白名单，不再携带内部文件路径或 Markdown 原文。
- 修复附件响应体、Obsidian 正则与当前 TypeScript 编译目标的兼容问题。
- 新增受管理员服务端鉴权保护的 `/admin/knowledge` 只读状态页，显示短版本、解析时间、笔记数量与脱敏诊断；沿用后台“刷新数据”按钮重新读取状态。
- 环境示例和使用说明补充知识库本地路径覆盖、服务器默认目录及 Git 运行依赖；设计和交互合同记录知识库的索引、阅读与权限规则。

## 2026-09-10：启用 Obsidian 服务器只读同步

- 为 `dongjinyue/obsidian-vault` 配置仓库专用的只读 Deploy Key（部署密钥），服务器不能用该密钥向仓库写入内容。
- 将私有知识库克隆到腾讯云服务器的 `/home/ubuntu/content/obsidian-vault`。
- 安装同步脚本和 systemd（系统服务管理器）定时器，每约 5 分钟以仅快进方式同步一次 `main` 分支。
- 同步遇到服务器本地修改时会停止，不会覆盖未知内容；失败保护和定时执行均已验证。
- 网站目前仍未读取或公开知识库内容，本次只完成服务器侧私有副本同步。

## 2026-09-09：建立 Obsidian 个人知识库

- 将 Obsidian 安装到 `D:\my\Obsidian`，知识库独立存放在 `C:\Users\24315\Desktop\AI\obsidian-vault`。
- 创建编程、AI、项目笔记目录，以及统一的附件目录、笔记模板、知识库首页和中文学习指南。
- 内容笔记模板默认使用 `visibility: private` 和 `status: draft`，避免新笔记意外公开。
- 初始化独立 Git 仓库，并首次推送到 `dongjinyue/obsidian-vault` 私有 GitHub 仓库。
- 网站读取知识库和服务器自动同步仍未实施，本次不把知识库描述为已经上线。

## 待发布：列表分页页码

- 项目管理、工具管理和分类管理统一增加可直接跳转的页码，并保留上一页、下一页。
- 工具集对筛选后的结果每页显示 8 个工具；修改搜索词或分类时自动回到第 1 页。
- 修复创建工具时因游客可见性字段缺少数据库新增权限而保存失败的问题。

## 待发布：文档与项目状态同步

- 将默认 Next.js README（项目说明）替换为 MY SPACE 的功能、技术栈、环境变量、运行和验证说明。
- 将学习路线从初始方案改为“已完成、待验证、下一阶段”的进度文档。
- 补充仓库内 Agent（编码智能代理）规则、设计令牌映射、UX（用户体验）权威来源和当前限制。
- 统一说明 AI 新闻仍为示例内容，部署、域名与线上任务需在目标环境单独核验。

## 待发布：工具与项目卡片优化

- 工具集改为桌面四列的紧凑文字卡片，保留标签与“访问工具”入口，并在平板和手机下降为两列、单列。
- 项目没有真实封面时不再显示大面积蓝色字母占位图，改为展示补充摘要和最多两条项目亮点。
- 修复鼠标移出“项目”导航后，下拉菜单偶尔因链接保留焦点而不消失的问题；键盘可见焦点仍能正常展开菜单。

## 2026-09-06：统一项目与工具可见性

- 恢复“项目”顶部悬停菜单，展示最近三个当前账号可见的项目并保留完整列表入口。
- 项目和工具统一使用“游客可见 / 仅登录可见”控制，新建内容默认游客可见。
- 新增项目 `hide_from_guests` 字段；旧私有项目自动迁移为仅登录可见，登录用户仍可浏览列表和详情。
- 未勾选列表项时，批量按钮使用明确的禁用样式和 `not-allowed` 指针，不再显示等待转圈。

## 2026-09-06：导航性能与游客可见性

- 恢复顶部“项目”入口，同时移除全站布局中不再需要的项目菜单查询。
- 全站身份、工具和分类数据改为并行读取；同一次渲染复用工具查询，并为工具页增加即时加载反馈。
- 新增 `hide_from_guests` 字段：游客看不到被隐藏工具，登录用户仍然可以看到。
- 旧的隐藏工具自动迁移为“仅登录可见”，新工具默认“游客可见”。
- 后台工具列表和编辑表单增加“游客可见 / 仅登录可见”控制及清晰确认文案。

## 2026-09-06：首页科技简报版

- 按确认的蓝白科技简报图重做首页：日期横幅、新闻筛选列表、主题缩略图和右侧补充栏目。
- 移除旧首页欢迎大标题、项目展示和重复工具入口，工具仍从顶部分类菜单直达。
- 顶部增加 AI 新闻入口，保留登录/后台管理的身份判断；新增 /news 完整示例列表。
- 分类筛选、正文展开、更多新闻和关注条目跳转可用，适配桌面与手机布局。
- 目前新闻均为明确标识的示例；尚未实现每日自动采集、真实新闻来源和历史简报归档。
- 同步更新全局品牌色和设计说明，保留原有后台与数据库数据。

## 工具集与分类同步

- 工具分类改为独立数据，可在后台完成新增、查看、改名和删除。
- 分类改名会自动同步关联工具；仍有工具使用的分类不允许删除。
- 工具表单的分类单选项从分类表动态读取，不再写死 AI、开发、学习、效率。
- 工具集筛选按钮和顶部工具集大型菜单直接读取公开分类表。
- 新增但暂时没有工具的分类也会正常显示，并标记为“暂无公开工具”。
- 顶部工具菜单将分类标题与工具内容拆成上下两层，分类使用独立底色、标签和分隔线，空分类提示降低视觉权重。
- 修复分类表的公开读取权限，确保访客页面可以同步最新分类。
- 现有分类和工具数据通过数据库迁移保留，不做删除式重建。

## 验证

- ESLint（代码规范检查）通过
- TypeScript（类型检查）通过
- Next.js 生产构建通过
- frontend-design-premium 严格 UI 审计通过（0 个问题）
- Supabase（数据库服务）迁移已正式应用
