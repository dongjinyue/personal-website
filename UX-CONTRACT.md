# MY SPACE UX Contract

## 范围与依据

本文件记录公开内容浏览和 Admin（后台管理）的可观察交互行为，不复制数据库策略。权限、数据生命周期和删除约束以 `lib/auth/admin.ts`、Supabase migration（数据库迁移）与服务端 actions（操作）为准。

## 导航

- 公开主导航固定使用“首页、AI 新闻、项目、工具集”，当前分区通过 `aria-current="page"` 标识。
- 手机端导航使用可展开菜单；选择目标后关闭，不依赖 Hover（鼠标悬停）。
- 页面标题使用“页面 | MY SPACE”语义；不存在的详情页提供明确的 404（未找到）状态。
- Admin 表单存在未保存内容时，所有站内显式导航继续经过 `UnsavedChangesProvider`。

## 内容浏览

- 项目列表只展示公开项目，保持服务端分页；项目卡片先进入 `/projects/[slug]`。
- 工具列表只读取已发布工具；游客看不到 `hide_from_guests = true` 的记录，登录用户仍可看到。筛选在客户端执行，工具链接直接安全地打开外部网站。
- 首页使用明确标识的 AI 新闻示例，不重复展示项目或工具卡片；项目与工具从顶部导航进入。
- 空数据与无搜索结果使用不同文案；无搜索结果提供清除筛选操作。

## Admin 列表与 CRUD

- 项目、工具均使用每页 10 条的服务端分页；删除后服务端重新计算有效页码。
- 桌面端使用语义化宽表格，表格区域横向滚动并固定右侧操作列；760px 以下切换为逐条管理卡片，保留相同状态、更新时间和操作。
- 项目表格支持选择当前页项目，并通过“设为公开 / 设为隐藏”批量修改可见性；公开前必须再次确认，服务端逐条校验管理员身份和记录版本。
- 工具表格批量操作控制“游客可见 / 仅登录可见”；旧的隐藏工具迁移为仅登录可见，新建工具默认游客可见。该控制不删除数据，登录用户不受影响。
- 新建和编辑继续使用现有共享表单；成功后回到所属列表并显示状态消息，失败时保留输入。
- 永久删除必须使用应用内确认弹窗；显示对象与后果，初始焦点为“取消”，成功前弹窗保持打开且禁止重复提交。
- 公开状态切换沿用编辑页的独立确认流程；存在未保存修改时不可切换。

## 弹窗与焦点

- 统一由 `ConfirmDialog` 负责原生模态语义、Escape 取消、视口居中、遮罩和安全边距。
- 危险确认不通过点击遮罩关闭；关闭后由触发组件把焦点恢复到原按钮。
- 处理中两个操作按钮均禁用，状态通过 `aria-live` 文本说明。

## 响应式与无障碍

- 支持 1440、1024、768、390px；页面主体不产生意外横向滚动，只有桌面数据表包装层可横向滚动。
- 交互使用原生链接或按钮，提供可见键盘焦点；重要手机触控目标至少 44px 高。
- 状态使用文字与徽标共同表达，不只依赖颜色。
- `prefers-reduced-motion: reduce` 时取消非必要位移动效。
- 公共导航在访客状态显示“登录”；仅在服务端确认当前会话属于管理员后，将该入口替换为“后台管理”。
- “项目”是直接列表入口；“工具集”使用按真实分类分栏的大型菜单，并保留“查看全部”入口。游客菜单排除仅登录可见工具，登录后的菜单包含这些工具。
- `/admin` 不再承载概览，访问后直接进入 `/admin/tools`；后台侧栏只保留工具管理与项目管理。
- 工具分类在后台“分类管理”中独立完成新增、查看、改名和删除；工具表单只允许选择现有分类。分类改名会同步相关工具，仍被工具使用的分类禁止删除。公开工具页筛选项及顶部工具菜单均从公开数据动态生成，不维护写死的分类清单。

## 导航性能

- 全站布局并行读取登录声明、可见工具和分类，不串行等待独立请求。
- 同一次服务端渲染中的工具与分类查询复用结果；工具页切换期间立即显示带 `aria-busy` 的加载状态。
- 公开工具页不重复执行 Proxy（中间代理）会话刷新；敏感后台路由仍由服务端重新验证管理员权限。
